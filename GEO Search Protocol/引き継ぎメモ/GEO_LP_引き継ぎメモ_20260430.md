# GEO LP 引き継ぎメモ（2026年4月30日）

---

## 本日行った主な作業

### 共通（AiscanLP.tsx / AiscanLP_B.tsx）

**フッター**
- 背景 `#EEECEA` → `#000000`、文字色 `var(--muted)` → `rgba(255,255,255,0.6)`
- 「GEO Search Protocol」テキスト → 画像 → テキスト（`#FFFFFF`）に戻す
- フォントサイズ -2px（19→17 / 17→15 / 16→14px）

**モーダル**
- 表示位置：下から引き出し → 画面中央
- 高さ：`88svh`固定 → `auto`（`maxHeight: 72svh`）
- 角丸：`20px 20px 0 0` → `20px`、幅 `540` → `480`

**RESULTSセクション**
- 画像レイアウト：横並び（`grid 1fr 1fr`）→ 縦配列（`flex column`）
- 画像高さ：`maxHeight: 220` → `360`
- maxWidth：`760` → `900`
- セクション内フォント全体 +2px（構文エラー修正済み）
- VOICEカード内フォント +2px

**REALITY CHECKセクション**
- 背景：`rgba(255,45,107,0.03)` → `#000000`（透過オーバーレイ付き → 最終的に画像なし・`#000000`のみに変更）
- フォントをWHY NOWセクションに統一
- 文字色 `#FFFFFF`

**FV背景画像をセクションに反映**
- 対象：REALITY CHECK / HOW IT WORKS / WHAT IS GEO
- `fv-bg-a`（A案）/ `fv-bg-b`（B案）+ グラデーションオーバーレイを各セクションに適用
- REALITY CHECKは最終的に画像なし・`#000000`背景に変更

**背景オーバーレイ濃度調整**
- FV以外の画像セクション（HOW IT WORKS・WHAT IS GEO）
- `0.85/0.65/0.4` → `1.0/0.85/0.6`（+0.2、黒寄り）

**WHAT IS GEO h2**
- `color: '#FFFFFF'` 追加

---

### HOW IT WORKSセクション（両ファイル）

**コンテンツ全面変更**（「申し込みフロー4ステップ」→「GEO設計プロセス3ステップ」）
- h2：「申し込みから診断完了まで2営業日」→「なぜ、AIに推薦される状態を再現できるのか？」
- サブタイトル追加：「属人的なノウハウではなく、"構造"で設計しています。」
- STEP01：設計（検索意図の占有）
- STEP02：最適化（AIに理解させる）
- STEP03：接続（外部整合性）
- 各STEPに箇条書き＋ポイントバッジ（白ベース統一）
- 実績テキストボックス追加：「実際にこの設計で、Bar SECRETは推薦表示を獲得しています」
- CTA以外フォント +4px
- 本文フォントカラー `#FFFFFF` / `rgba(255,255,255,0.8)`

---

### AiscanLP_B.tsx のみ

**FV改行・CTAボタン調整**
- h1キャッチ：`display: 'block'` で各行を明示的に改行制御
- CTAボタンテキスト：「なぜ競合はAIに出て、自社は出ていないのか」+「【診断結果を受け取る】」の2行構成

---

## 現在のファイル状態

- 本スレッドで作業した最新ファイルが手元に存在
- push済み・Vercelデプロイ確認済み

---

## 継続保留事項（前回から引き継ぎ）

- indexing-submit.js 実行（岐阜・東京23区分）
- AiscanLP_B_seminar.tsx：セミナー準備完了次第 AiscanLP_B.tsx と差し替え
- seminar-banner-b.webp・free-badge.webp push待ち
- 全ページへのAI生成独自テキスト追加（Gemini API連携）
- 顔写真・代表メッセージ追加（初回成約後）
- 信頼性セクション追加（導入実績・企業名・お客様の声）
- GA4で `form_submit` をコンバージョン設定

---

## Pushコマンド（共通）

```bash
cd ~/Downloads/geo-lp
cp ~/Downloads/AiscanLP.tsx components/AiscanLP.tsx
cp ~/Downloads/AiscanLP_B.tsx components/AiscanLP_B.tsx
git add components/AiscanLP.tsx components/AiscanLP_B.tsx
git commit -m "メッセージ"
git push origin main
```

## Cookie操作（A/B確認）

```javascript
// B案に切り替え
document.cookie = "ab_variant=B; path=/; max-age=2592000"
location.reload()

// A案に戻す
document.cookie = "ab_variant=A; path=/; max-age=2592000"
location.reload()
```


[[GEO_LP_引き継ぎメモ_20260401]]
[[GEO_LP_引き継ぎメモ_20260410]]
[[GEO_LP_引き継ぎメモ_20260419b]]
[[GEO_LP_引き継ぎメモ_20260420]]
[[GEO_LP_引き継ぎメモ_20260420_afternoon]]
[[GEO_LP_引き継ぎメモ_20260428]]
